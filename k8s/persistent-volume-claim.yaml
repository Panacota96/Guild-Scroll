apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: guild-scroll-sessions-pvc
  namespace: guild-scroll
  labels:
    app: guild-scroll
    component: storage
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
